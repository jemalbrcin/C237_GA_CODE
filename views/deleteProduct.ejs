<!DOCTYPE html>
<html lang="en">
<head>
  <title>Jam Station</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    nav ul {
    margin-left: 120px;
    margin-right: 120px;
  }
  
  .main-title {
    height: 45px;
  }

  nav li {
    margin-left: 30px;
    font-size: 18px;
  }

  .search-bar {
    width: 400px;
  }

  body {
    font-family: Georgia, 'Times New Roman', Times, serif;
    background-color: #f1f1f1;
  }

  .carou-pic {
    margin-left: 320px;
    width: 60%;
  }

  h2 {
    font-size: 50px;
  }

  

  </style>
</head>
<body>

<nav class="navbar navbar-expand-sm bg-secondary navbar-dark justify-content-center main-title">
  <div class="navbar-nav">
    <a class="navbar-brand" href="/"><h3>Jam Station<h3></a>
  </div>
</nav>

<div class="container">
    <br><br>
    <div class="text-center"><h2>Delete Product</h2></div>
    <br>
    <div class="row">
        <% for(let i = 0; i < products.length; i++) { %>
            <div class="col-md-4 mb-4">
                <div class="card" style="width: 100%">
                    <img src="/image/<%= products[i].image %>" class="card-img-top" style="width:50%">
                    <div class="card-body">
                        <h4 class="card-title"><%= products[i].name %></h4>
                        <h5 class="card-title">$<%= products[i].price %></h5>
                        <p class="card-text">Brand: <%= products[i].brand %></p>
                        <a href="/deleteProduct/<%= products[i].productID %>" onclick="return confirm('Are you sure you want to delete this Product?')" class="btn btn-danger" style ="width: 100%">Delete</a>
                    </div>
                </div>
            </div>
            <% if((i + 1) % 3 === 0) { %>
                </div><div class="row">
            <% } %>
        <% } %>
    </div>
</div>